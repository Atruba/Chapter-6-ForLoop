#include <iostream>
#include <string>
using namespace std;

int main()
{
	int c;
	cout << "Kindly enter a number to find out the cube" << endl;
	cin >> c;
	for (int x = 1; x <= c; x++)
	{
		cout << "\nNumber is: " << x << "and the cube is ";
		int y = x;
		y = y * y * y;
		cout << y;
	}
}
